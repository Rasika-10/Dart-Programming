
//A variable that can be assigned once and cannot be changed afterwards.
//final = set once at runtime
void main()
{

    final name="Rasika";
    //name="Adhi";  // ❌ Error: final variable can't be reassigned
    print(name);
}